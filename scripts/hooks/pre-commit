#!/bin/sh
# Git pre-commit hook running secret-scan.ps1 on staged files
if command -v pwsh >/dev/null 2>&1; then
    pwsh -NoProfile -ExecutionPolicy Bypass -File "$(dirname "$0")/pre-commit.ps1"
    exit $?
fi
