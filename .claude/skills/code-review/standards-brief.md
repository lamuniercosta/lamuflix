Report — per file/hunk where relevant — (a) every place the diff violates a documented standard: cite the standard (file + the rule); and (b) any baseline smell you spot: name it and quote the hunk. Distinguish hard violations from judgement calls — documented-standard breaches can be hard, baseline smells are always judgement calls capped at Medium, and a documented repo standard overrides the baseline. Skip anything `dotnet format` or Roslyn analyzers enforce. For each finding give `file:line` and the concrete maintenance cost it imposes. Keep a finding only with a cited documented rule or a named baseline smell quoted from the hunk; otherwise ask a question or drop it. Challenge unnecessary scope and abstraction before proposing a correction. Report findings meeting the evidence threshold, up to 15. Say so if any evidence-backed findings were cut. Assign severity per this skill's Severity table (`SKILL.md` `## Severity`, in the directory containing `SKILL.md`).

Identify and read the repo's documented standards sources where they exist: `CODING_STANDARDS.md`, `CONTRIBUTING.md`, the conventions sections of `CLAUDE.md`/`AGENTS.md`, `.cursor/rules/*.mdc`, `harness.yml` (the gate thresholds), and — .NET-specific — `.editorconfig` (style + analyzer severities), `Directory.Build.props` (`AnalysisLevel`, `AnalysisMode`, `TreatWarningsAsErrors`, analyzer package references), `BannedSymbols.txt`, and any `.globalconfig`.

Read the smell baseline from `./smell-baseline.md` in the directory that contains this skill's `SKILL.md` and include it in your review.
