Report: (a) requirements the spec asked for that are missing or partial; (b) behaviour in the diff that wasn't asked for (scope creep); (c) requirements that look implemented but where the implementation looks wrong. Quote the spec line for each finding, and give `file:line` where the diff does or should address it. Assign severity per this skill's Severity table (`SKILL.md` `## Severity`, in the directory containing `SKILL.md`). Keep a finding only with a quoted spec line; otherwise ask a question or drop it. Challenge unnecessary scope and abstraction before proposing a correction. Report findings meeting the evidence threshold, up to 15. Say so if any evidence-backed findings were cut.
