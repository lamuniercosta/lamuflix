# DEV-360 research

- `LamuFlixContext.OnConfiguring` reads `LAMUFLIX_CONNECTION` and falls back to localhost (`LamuFlix.Data/LamuFlixContext.cs:31-41`). `UnitTest1.SetUp` has the test fallback (`LamuFlix.Test/UnitTest1.cs:32-41`). Host configuration already reads user secrets, so no host change is planned (Patron Q1; `task-DEV-360:15-18`). The direct environment read is not in the constitution's Known Debt list (constitution:421-441). Per Patron's C1/C2 ruling (`task-DEV-360:89`), plan.md's VII row records it as scoped, ticket-decided transitional behavior under constitution:426-429. It is pre-existing and not extended.
- `GenericRepository.Delete(object id)` returns if `Find(id)` is null (`GenericRepository.cs:35-44`), contrary to `task-DEV-360:19`. The repo has no `NotFoundException` type. Patron ruled, and the owner accepted, that `KeyNotFoundException` is retained (`task-DEV-360:83`).
- The snapshot maps Actor, Collection, Director, Genre, Movie, MovieActors, MovieDirectors, MovieGenre, and Player. It has nullable string columns and nullable `Movie.CollectionId`; join FKs are required. `Temp` has no `DbSet` and no snapshot entity, so it is unmapped and out of FR-004 scope. Patron Q2 governs navigation annotations. No migration (`task-DEV-360:20-24`). `Movie.Status` is mapped (`Movie.cs:21`; `LamuFlixContext.cs:54`, default `Pending`) but absent from the snapshot and every migration, so the model is expected to have one pending `AddColumn` for `Status`. This is not yet measured: the 2.1.3-era snapshot may yield other operations under EF Core 9, so T007 records the baseline (Patron SF-1). Patron H1: no migration (`task-DEV-360:89`). Owner option A (2026-09-23) amends AC4/SC-003: DEV-360 introduces no pending operation beyond that `AddColumn`, and the six `Id` IdentityColumn `AlterColumn` operations measured at T007/T014 are pre-existing baseline residuals. DEV-19's fresh Initial migration owns them.
- FR-004 nullability changes reach consumers in `LamuFlix.Web/Services/FilmesServices.cs` (`:74-75`, `:79`, `:85`, `:136`, `:314`, `:375`, `:388`) and may reach `UnitTest1.cs`. Under `TreatWarningsAsErrors` these need behavior-preserving fixes (Patron H2; tasks T013a). No snapshot string column is `IsRequired`, so every mapped string property (for example `Movie.Title`, `Location`, `Format`, the `Name` properties, and `Player.Formats`/`Path`) becomes `string?`. Other model consumers (`FilmesController.cs`, `EntityExtensions.cs`, `LamuFlix.Test/EnrichmentTests.cs` (assigns literals only), `LamuFlix.Data/Repositories/UnitOfWork.cs`, `LamuFlix.Work/Services/EnrichmentJobProcessor.cs`, `Worker.cs`, `Views/Filmes`) use only log arguments, `==` comparisons, and Razor output, which are not expected to warn. T005 re-checks them, and T013a's stop condition (Patron SF-4) covers any surprise.
- DB-dependent ignored tests are `TestMethod1`, `TestMethod2`, and `TestMethod5`. `TestMethod4` does not use `_dataContext` (`UnitTest1.cs:46-51,89-92,327-346`). `TestMethod1` reads the context (:50) before touching `F:/Filmes`.
- `Directory.Build.props:20` sets `TreatWarningsAsErrors=true`; `harness.yml:22` has `warningsAsErrors: false` (`task-DEV-360:25`).
- EF tooling, observed 2026-09-23 on this worktree with `LAMUFLIX_CONNECTION` unset:
  - Before restore, `dotnet ef migrations has-pending-model-changes …` exited 1 with NETSDK1004, because `project.assets.json` was missing (analysis U1).
  - `dotnet restore LamuFlix.Data` then exited 0. The same command (global `dotnet-ef` 10.0.5) built successfully and exited 1: "Your startup project 'LamuFlix.Data' doesn't reference Microsoft.EntityFrameworkCore.Design." (U2, now observed).
  - Versions: `Directory.Packages.props` pins EF Core and Pomelo at 9.0.0. `.config/dotnet-tools.json` has no `dotnet-ef`.
  - Microsoft's EF Core 9 breaking-changes page ("Microsoft.EntityFrameworkCore.Design not found when using EF tools") documents `Publish=true` beside `PrivateAssets=all` as the workaround for SDK ≥ 9.0.200. Side effect: the Design assembly is copied to output.
  - Owner decision D3 authorizes this package and a 9.0.x tool pin. quickstart §1 is the gate.
- `ProcessStarter` stays unchanged (owner decision D1; `task-DEV-360:29,39`).
- Gate baseline, measured by Patron with pwsh 7 on 2026-09-23 over the 16-file **gate-baseline set**, which is byte-identical to Phase B `main`. The set is `LamuFlix.Data/LamuFlixContext.cs`, `LamuFlix.Data/Repositories/GenericRepository.cs`, `LamuFlix.Web/Services/FilmesServices.cs`, `LamuFlix.Test/UnitTest1.cs`, and all 12 `LamuFlix.Data/Models/*.cs`. It is wider than the plan's file impact boundary, which excludes the unmapped `Temp.cs` and `MovieEnrichmentMessage.cs`.
  - `run-roslyn-analyzers.ps1 -Files …` exit 0.
  - `run-cyclomatic-complexity.ps1 -Files …` exit 0 at both 15 and `-Threshold 6`.
  - `run-jetbrains-inspectcode.ps1 -Files …` exit 1, with one WARNING: `LamuFlix.Data/Models/MovieEnrichmentMessage.cs:1` `RedundantNullableDirective`. The file is unmapped and tracked by DEV-366.
  - Under Windows PowerShell 5.1 the complexity script crashes (a harness defect, reported to Bernstein), so use pwsh 7. T005 re-measures at pickup.
- Current chain order is DEV-360 before DEV-290 and DEV-280 (`chain:7-11`). Recheck at pickup.

## Phase B pickup evidence (T005, T007, T014)

Measured 2026-09-23 on `feature/DEV-360` at `F:\Dev\LamuFlix.worktrees\DEV-360` with pwsh 7.6.6. DEV-280 and DEV-290 have not landed on `origin/main` (no commits). EF Core and Pomelo remain `9.0.0`. Cited lines in `LamuFlixContext.cs`, `UnitTest1.cs`, `GenericRepository.cs`, and `FilmesServices.cs` still match the Phase A citations. No new pre-existing method over 6, and none over 15. Out-of-set consumers (`FilmesController.cs`, `EntityExtensions.cs`, `EnrichmentTests.cs`, `UnitOfWork.cs`, `EnrichmentJobProcessor.cs`, `Worker.cs`, `Views/Filmes`) still use log arguments, `==` comparisons, or Razor output; the build after FR-004 warned only in `FilmesServices.cs`. InspectCode on the changed files then reported `FilmesServices.cs:106` `RedundantCast` on the pre-existing `(string?)` cast of `Player.Formats`, which FR-004 made redundant. That cast was removed; the null check and `Contains` call remain.

T005 gate-baseline set (the same 16 files), pwsh 7:

- `run-roslyn-analyzers.ps1 -Files …` exit 0.
- `run-cyclomatic-complexity.ps1 -Files …` exit 0 at 15 and at `-Threshold 6`.
- `run-jetbrains-inspectcode.ps1 -Files …` exit 1. One WARNING: `LamuFlix.Data/Models/MovieEnrichmentMessage.cs:1` `RedundantNullableDirective`. DEV-366 applies to this T005 16-file baseline only.

T007 is a baseline only. Quickstart §1 (pinned `dotnet-ef` 9.0.0, target framework `net8.0` from `dotnet-ef.runtimeconfig.json`; `Microsoft.NETCore.App` 8.0.22 is installed; manifest `rollForward: false`): `dotnet tool restore` 0, `dotnet tool run dotnet-ef --version` reports `9.0.0` exit 0, `dotnet restore LamuFlix.Data` 0, `dotnet list LamuFlix.Data package` shows `Microsoft.EntityFrameworkCore.Design` 9.0.0, Web and Work transitive graphs do not list that package, `dotnet build LamuFlix.Web` 0, `dotnet build LamuFlix.Work` 0, and neither host `bin` contains `Microsoft.EntityFrameworkCore.Design.dll`. `LamuFlix.Data.deps.json` lists the package; the design DLL was not copied into `LamuFlix.Data/bin` on this SDK. Quickstart §5 before T013, with a process-only dummy `LAMUFLIX_CONNECTION` (not committed): `has-pending-model-changes` exit 1, "Changes have been made to the model since the last migration." Probe `migrations add Dev360PendingProbe` exit 0. `Up` was not only `AddColumn` `Status`. It also contained `UpdateData` plus `AlterColumn` (nullable false) for `player.Path`, `player.Name`, `player.Formats`, `movie.Title`, `movie.Location`, `movie.Format`, `genre.Name`, `director.Name`, `collection.Name`, and `actor.Name`, and `AlterColumn` `Id` adding `MySql:ValueGenerationStrategy` IdentityColumn on `player`, `movie`, `genre`, `director`, `collection`, and `actor`. The probe was discarded. `git status --short LamuFlix.Data/Migrations` was empty.

T014 owns the SC-003 verdict, after FR-004 nullability. `has-pending-model-changes` exit 1 with the same pending-changes text. Probe `Up` still contains operations other than `AddColumn` `Status`: `AlterColumn` `Id` with `MySql:ValueGenerationStrategy` IdentityColumn on `player`, `movie`, `genre`, `director`, `collection`, and `actor`, plus `AddColumn` `Status` on `movie` (`int`, non-nullable, `defaultValue: 0`, which is `MovieEnrichmentStatus.Pending`). The string `AlterColumn`/`UpdateData` operations from the T007 baseline are gone. The probe was discarded; no migration is committed. That failed SC-003 as it was worded before the amendment (exactly one operation), so DEV-360 stopped here for Patron, with this T007 baseline. No migration was added. The owner then answered option A (spec.md Gate 1 "T014 block"). Under the amended SC-003, these six `Id` IdentityColumn operations are allowed baseline residuals. T014 is rerun only after the re-freeze.

T014 rerun after re-freeze `fcfd3e8` (2026-09-23): `has-pending-model-changes` exit 1, text "Changes have been made to the model since the last migration. Add a new migration." Probe `migrations add Dev360PendingProbe` exit 0. `Up` operations, and only these: `AlterColumn` `Id` IdentityColumn on `player`, `movie`, `genre`, `director`, `collection`, and `actor`; `AddColumn` `Status` on `movie` (`int`, nullable false, `defaultValue: 0`, which is `MovieEnrichmentStatus.Pending`). Probe files deleted and `LamuFlixContextModelSnapshot.cs` restored. `git status --short LamuFlix.Data/Migrations` was empty. No migration was added. Amended SC-003 is met.

T016: Ledger was not connected. The review used `.claude/skills/code-review/standards-brief.md`, `smell-baseline.md`, and the vendor rules on the DEV-360 diff and on the DEV-289 file-set items already enumerated on `main`. No Critical or High finding with a concrete failure scenario is inside the frozen DEV-360 scope. Recorded, not fixed, and not scheduled: `GenericRepository.cs:91` `IGenericRepository.GetById` throws `NotImplementedException` (smell baseline Refused Bequest, Medium cap, pre-existing, not a Q3 annotation edit); `FilmesController.cs:28` and `Worker.cs:37,57` `DateTime.Now` remain DEV-289's blocked scope item 6; `MovieEnrichmentMessage.cs:1` `RedundantNullableDirective` remains DEV-366 and applies to the T005 baseline only.

T017: no DEV-289-set `.cs` file outside the gate-baseline set was edited, so no pre-edit gate addendum was required. No additional Q3-local fix. The `FilmesServices.cs:106` redundant-cast removal is the T013a edit already in `cd26a74`.

`propertyTests: opt-out — no new domain invariant in DEV-360`

T018 numeric exits, pwsh 7.6.6, changed `.cs` files versus `origin/main` (10 files): Roslyn 0; cyclomatic complexity 0 at 15 and 0 at `-Threshold 6`; InspectCode 0 (DEV-366's `MovieEnrichmentMessage.cs:1` warning is outside this changed-file set); property tests 2 (`SKIPPED`, no `Category=Property` tests; accepted under the task-note opt-out above); vulnerable packages 0; `dotnet format --verify-no-changes` 0; `dotnet test` 0 (18 passed, 4 skipped); gitleaks 8.30.1 `detect` 0 (`no leaks found`). `scripts/run-web-gates.ps1` is not in the repo and there is no `web/` tree or `web/` diff, so the web gate is `SKIPPED` under Patron SF-7 rather than PASS. T015 was not re-run; `harness.yml` `warningsAsErrors` remains `true`, matching `Directory.Build.props`.

T019: `git diff origin/main` paths stay inside the file-impact boundary plus the option A spec commits. `ProcessStarter` is unchanged. `FilmesServices.cs` additions are the T013a null-forgiving and `Formats` null-check edits at lines 74, 75, 106, 126, 136, 181, 308, 314, 375, and 388. `LamuFlix.Data/Migrations` has no diff. No test project or package was added.
