# DEV-361 verification scenarios

1. Compare `.editorconfig` with `spec.md` FR-001: exactly five target keys read `warning`; the B1 key stays `suggestion` unless the user answered.
2. Confirm only `FilmesViewModel.cs`, `TagHelpers/Extensions.cs`, and `UnitTest1.cs` are edited for inspection fixes. `FilmesFilterViewModel` is populated by the `[FromQuery]` MVC model binder (`FilmesController.cs:25`); recheck assignment and binder evidence before suppressing any target hit.
3. Check the two equality sites retain their `null != qs.Value` guard and compare key/value fields, and the four legacy test conditions retain the non-null membership predicate.
4. Run `./scripts/run-jetbrains-inspectcode.ps1 -Files 'LamuFlix.Web/Models/Filmes/FilmesViewModel.cs','LamuFlix.Web/TagHelpers/Extensions.cs','LamuFlix.Test/UnitTest1.cs'` at default WARNING; record numeric exit 0 and review all WARNING+ findings in that scope.
5. Confirm each remaining suppression is at type/member scope with a writer reason and no new package reference. With `$files=@('LamuFlix.Web/Models/Filmes/FilmesViewModel.cs','LamuFlix.Web/TagHelpers/Extensions.cs','LamuFlix.Test/UnitTest1.cs')`, run gates sequentially with `-BaseRef origin/main`: `./scripts/run-roslyn-analyzers.ps1 -BaseRef origin/main -Files $files`, `./scripts/run-cyclomatic-complexity.ps1 -BaseRef origin/main -Files $files` at configured threshold 15, then `-Threshold 6` for DEV-361-changed methods at the refactor gate, and `./scripts/run-jetbrains-inspectcode.ps1 -BaseRef origin/main -Files $files -MinSeverity WARNING`. Also run `dotnet format --verify-no-changes` and `dotnet test`. Record numeric exits, findings, and any skip/opt-out distinctly; no skipped gate counts as a pass. No baseline exception is granted (T013 F2): if the three-file threshold-6 run exits non-zero, stop and report `blocked` to Patron with the emitted method, complexity and numeric exit (see `plan.md` step 7).
